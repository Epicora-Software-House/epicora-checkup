<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Testes da gravação. O que se verifica aqui é o CONTRATO do arquivo: um JSON fora do
    schema 1.0 não entra no consolidador, e o erro só apareceria semanas depois, no
    escritório, com a visita já encerrada.

    Alvo e plataforma vêm de ../Directory.Build.props (ADR-001).
  -->

  <PropertyGroup>
    <RootNamespace>EpicoraCheckup.Reporting.Tests</RootNamespace>
    <AssemblyName>EpicoraCheckup.Reporting.Tests</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\EpicoraCheckup.Reporting\EpicoraCheckup.Reporting.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

</Project>
